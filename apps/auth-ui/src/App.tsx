import React, { Suspense, lazy, useEffect, useState, useRef } from "react";

import "./App.css";

function App() {
  return (
    <div className="ol-text-2xl ol-font-bold ol-text-[hsl(var(--brand))]">
      auth-ui sees brand color ✅
    </div>
  );
}

export default App;
